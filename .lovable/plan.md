План доработки

1. Первый экран и фотография мастера
- Оставить текущую удачную композицию hero: позу, одежду, взгляд, выражение лица и локацию не менять.
- Перегенерировать/отредактировать hero-фото по вашим новым референсам:
  - лицо сделать ближе к эксперту с фото 2;
  - выровнять глаза, убрать асимметрию;
  - на мастере разместить украшение с фото 1;
  - не менять общий кадр, свет, позу и настроение.
- После замены проверить, чтобы на десктопе и мобильной версии лицо не обрезалось слишком крупно.

2. Шрифт и типографика
- Заменить текущий каллиграфический акцент для слова «Украшения» и брендинговых элементов на более изящный script-шрифт, визуально ближе к надписи Lana Stone на фото 3.
- Ограничить сайт максимум тремя шрифтами:
  - новый каллиграфический для акцентов;
  - Cormorant Garamond для заголовков/подзаголовков;
  - Inter для обычного текста.
- Фразу «Созданные с вниманием к материалу, настроению и красоте каждого образа» сделать красивой курсивной акцентной строкой, согласованной с остальными подзаголовками.
- Обновить логотип/текст Lana Stone в шапке и на странице, чтобы он был ближе к стилю фото 3.

3. Декоративные элементы по сайту
- Убрать декоративное размещение прямоугольных фотографий браслетов как фоновых акцентов.
- Из новых/имеющихся фото сделать именно вырезанные изделия на прозрачном фоне: без фона, без прямоугольной фотографии.
- Разместить эти украшения аккуратно в углах секций как небольшие полупрозрачные акценты, кроме первого экрана.
- Уменьшить визуальный шум: декор должен выглядеть как лёгкие детали, а не как разбросанные товарные фото.

4. Онлайн-примерка
- Проверить текущий путь работы примерки: вход пользователя → загрузка фото → выбор изделия → запрос AI → сохранение результата → показ в галерее.
- Исправить вероятную причину поломки: серверная функция сейчас требует authorization header, а клиентский вызов может не передавать токен явно.
- Обновить вызов примерки так, чтобы токен авторизованного пользователя передавался корректно, либо адаптировать серверную функцию к текущему механизму TanStack Start.
- Улучшить сообщения об ошибках: отдельно показывать проблемы входа, загрузки, AI-лимита, сохранения и сетевые ошибки.

5. Проверка качества
- Запустить сборку/проверку TypeScript после правок.
- Проверить hero и основные секции на мобильной и десктопной ширине.
- Проверить сценарий онлайн-примерки на уровне кода и сетевого запроса; если потребуется реальный AI-запрос, выполнить его после авторизации в превью.

Технические детали
- Основные файлы: `src/components/lana/Landing.tsx`, `src/styles.css`, `src/components/lana/Logo.tsx`, `src/components/lana/TryOnSection.tsx`, `src/lib/tryon.functions.ts`.
- Ассеты: обновить `hero-fullscreen.jpg`, добавить вырезанное украшение/декор на прозрачном фоне, при необходимости заменить старые `real-bracelet-*.png`.
- Для генерации и редактирования изображений использовать Lovable AI через безопасный backend/скриптовый процесс, не вынося ключи на клиент.